#include "../entity/banca.cc"
#include <list>

class Jogo {
    private:
        Banca banca;
        std::list<Jogador> jogadores;
        int rodada;
        bool fim = false;
    public:
        
};